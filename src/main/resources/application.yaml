spring:
  application:
    name: gamebox

  datasource:
    url: jdbc:postgresql://localhost:5433/gamebox
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true